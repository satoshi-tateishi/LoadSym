-- L字（袖付き卓）やコの字（ラック）を外形矩形だけで判定すると、凹みに入るはずの機材まで入らなくなるため、機材の形を保存できるようにする。
-- 形は軸平行な矩形パーツの集合として持ち、既存の吸着・押し出し・空き探索を引き続き利用できるようにする。
-- width_mm / depth_mm は一覧表示・CSV・並び替え・空き探索の初期サイズに使う外形bboxとして残す。
-- parts[].kind を最初から持たせ、将来の多角形・円の追加時にマイグレーションが不要な形式にする。

alter table public.equipments add column shape jsonb;
